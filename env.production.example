# ===========================================
# Portrait 用户画像系统 - 生产环境变量配置
# ===========================================
# 使用方式: 复制此文件为 .env 并修改配置
# cp env.production.example .env
# ===========================================

# -------------------------------------------
# 服务端口配置
# -------------------------------------------
# 前端服务端口 (Nginx)
WEB_PORT=80
# 后端 API 端口
API_PORT=8000
# PostgreSQL 端口 (可选暴露，生产环境建议不暴露)
# POSTGRES_PORT=5432

# -------------------------------------------
# PostgreSQL 画像数据库配置
# -------------------------------------------
POSTGRES_USER=portrait
POSTGRES_PASSWORD=your_secure_password_here
POSTGRES_DB=portrait

# -------------------------------------------
# MySQL 源数据库配置 (连接线上外呼系统，只读)
# -------------------------------------------
# ⚠️ 重要: 确保云服务器能访问 MySQL 源库
# 如果源库在内网，需要配置 VPN 或安全组
MYSQL_HOST=your_mysql_host
MYSQL_PORT=3306
MYSQL_USER=readonly_user
MYSQL_PASSWORD=your_mysql_password
MYSQL_DB=outbound_saas

# -------------------------------------------
# 定时任务调度器配置 (可选)
# -------------------------------------------
# 是否启用定时任务
SCHEDULER_ENABLED=false
# 数据同步时间 (每天凌晨2点)
SYNC_CRON_HOUR=2
SYNC_CRON_MINUTE=0

# -------------------------------------------
# 其他配置 (可选)
# -------------------------------------------
# 日志级别
# LOG_LEVEL=INFO
